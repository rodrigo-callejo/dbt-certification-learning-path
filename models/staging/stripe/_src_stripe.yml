version: 2

sources:
  - name: stripe
    tables:
      - name: payment
        config:
          freshness:
            warn_after:
              count: 5
              period: day
            error_after:
              count: 20
              period: day
          loaded_at_field: _batched_at