sources:
  - name: jaffle_shop
    database: RODRIGO_CALLEJO_DB
    schema: jaffle_shop
    freshness:
      warn_after:
        count: 6
        period: day
      error_after:
        count: 30
        period: day
    loaded_at_field: _ETL_LOADED_AT

    tables:
    - name: orders
    - name: customers
      freshness: null
